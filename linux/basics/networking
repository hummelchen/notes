ifconfig - информация о сконфигурированных сетевых устройствах
	-a - все имеющиеся устройства
		Link encap - тип инкапсуляции
		HWaddr - mac-адрес
		inet addr - IP 	адрес, IPv4
		!address family - семейства адресов - inet - IPv4, inet IPv6, ipx IPX/SPX, link - все низкоуровневые протоколы, unix - пути в файловой системе
		netstat -a(все порты) --inet(семейство адресов)
		Bcast - широковещательный адрес Mask:-маска сети
		Флаги - UP - поднят, BROADCAST - принимает широковещательные пакеты
		MTU - максимальный размер пакета без фрагментации
	Статистика
		RX - принятые пакеты
		TX - переданные пакеты
		txqueuelen - длина очереди передачи
		Статистика в байтах
	ethX:0 192.168.0.1/24
lo - не имеет глубины стека драйверов сетевых интерфейсов
минимальная латентность
MTU:16436 
________________________________
ifconfig iface address(+number)/mask
Destination - направление, Genmask - пятно, Gateway - шлюз, iface - интерфейс, 
Флаги: U - маршрут пригоден к использованию, H - ведёт к одному единственному хосту, G - использование шлюза.
Чем точнее маршрут, тем больший приоритет он имеет.
route add default(0.0.0.0) gw(gateway) 10.5.10.254
/etc/resolv.conf
{
nameserver(dns) x.x.x.x
}
/etc/network/interfaces
{
auto eth0 - конфигурация при старте системы
iface eth0 inet static - статические настройки
	adress x.x.x.x
	netmask x.x.x.x
	gateway x.x.x.x
}
/etc/hostname - имя хоста
/etc/hosts - соответствие IP адресов именам хостов
/etc/init.d/networking - скрипт инициализации
arp - просмотр и настройка таблицы соответствия mac и ip-адресов address resolution protocol, arp-запрос, arp-таблица
	-a [hostname] соответствие mac-адреса и ip-адреса для указанного хоста
	-d [hostname] удалить запись из таблицы
	-s [hostname] [mac] вручную добавить запись в таблицу
arping -
ping 
	-R сохранение маршрута в передаваемых пакетах
	-b разрешить широковещательную рассылку
	-c число отправляемых пакетов
	-i интервал между отправкой пакетов
	-s размер пакета
traceroute - отслеживание маршрута
	-n отключить преобразование ip-адреса в dns имя
	-m максимальное количество контрольных точек(хопов), через которые будет идти пакет
netstat статистика по сетевым интерфейсам, отчёты по сетевым подключениям, службам и маршрутизации пакетов
	-n отключить преобразование ip-адреса в dns имя
	-l показать порты, открытые для прослушивания
	-i показать статистику по интерфейсам
	-r показать таблицу маршрутизации
	-s показать статистику по каждому протоколу
	-p показывает имя и PID программы
route -n(numerical) -e(symbolic) - таблица маршрутизации
	add - добавление маршрута в таблицу маршрутизации
	  -net -добавление маршрута к сети
	  -host добавление маршрута к хосту
	  -default добавление маршрута по умолчанию
	del - удаление маршрута из таблицы
	gw - указание адреса шлюза 
host - ip адрес домена
nslookup - взаимодействие с dns
tcpdump - прослушивание трафика
	-i интерфейс
	host [host]
iptraf - мониторинг сетевого трафика
wireshark - сниффер
