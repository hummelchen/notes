cd
    cd - : вернуться обратно
    cd : перейти в домашний каталог
        -v : вербозность
        -i : интерактивность
        -r : рекурсивность
        -u : обновление
        -a : скрытые обьекты
        -A : скрытые обьекты без . и ..
        -l : подробный вывод
        -F : с классификацией файлов * - исполняемый файл, | - пайп, = - сокет, / - директория, @ - именованная ссылка
        -d : показать только саму папку без содержимого
        -G : не выводить информацию о группе объекта
man
    Поиск мануала может идти по по разделам, например
    man 5 passwd
mkdir
    -p : создавать родительские каталоги
uptime - аптайм системы
halt - остановка системы
touch
    -c : не создавать файл, если его нет
